function registerProjectSocket (io) {
  // Placeholder for project-related socket.io events
  // Implement room join and task update broadcasts here later
  io.on('connection', () => {
    // No-op for now
  })
}

module.exports = { registerProjectSocket }


